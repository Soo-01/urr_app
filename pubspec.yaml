name: exo_tablet_app_v6
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.5.3

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  provider: ^6.1.1

  battery_plus: ^6.1.0
  # flutter_bluetooth_serial: ^0.4.0
  flutter_bluetooth_serial:
    path: packages/flutter_bluetooth_serial
  file_picker: ^8.1.4   # 6.1.1
  csv: ^5.0.1
  path_provider: ^2.1.2
  permission_handler: ^11.0.0
  fl_chart: ^0.64.0
  flutter_tts: ^3.8.4
  shared_preferences: ^2.1.1
  flutter_launcher_icons: ^0.13.1

  # for game
  flame: ^1.35.1  # ^1.36.0
  flame_audio: ^2.1.1


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  google_fonts: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
    

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^4.0.0

flutter_icons:
  android: true
  ios: true
  image_path: "assets/urr_logo.png"   # assets/exo.png
# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  generate: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/ONESTEP_LEVELWALKING_0.7vel_L_fit.csv
    - assets/ONESTEP_LEVELWALKING_0.7vel_R_fit.csv
    - assets/StandToSit_STS_L_fit.csv
    - assets/StandToSit_STS_R_fit.csv
    - assets/Stand_bringLeg_L.csv
    - assets/Stand_bringLeg_R.csv
    - assets/walk_L_1st.csv
    - assets/walk_R_1st.csv

  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

    # Game assets - Kenney Space Shooter (paddle, ball, power-ups, effects)
    - assets/images/kenney_space-shooter-redux/PNG/
    - assets/images/kenney_space-shooter-redux/PNG/Power-ups/
    - assets/images/kenney_space-shooter-redux/PNG/Effects/
    - assets/images/kenney_space-shooter-redux/PNG/Lasers/
    - assets/images/kenney_space-shooter-redux/PNG/Meteors/
    - assets/images/kenney_space-shooter-redux/PNG/UI/
    # Game assets - Kenney UI Pack (sounds — audio stays in assets/)
    # - assets/kenney_ui-pack/Sounds/      #################################################
    # Game assets - SFX
    - assets/100-CC0-SFX/
    - assets/DM-CGS/WAV/
    # Game assets - Kenney Space Shooter Backgrounds
    - assets/images/kenney_space-shooter-redux/Backgrounds/
    # Game assets - Kenney Space Shooter Ships & Enemies
    - assets/images/kenney_space-shooter-redux/PNG/Enemies/
    - assets/images/kenney_space-shooter-redux/PNG/Parts/
    - assets/images/kenney_space-shooter-redux/PNG/Damage/
    # Game assets - Kenney Medieval RTS
    - assets/images/kenney_medieval-rts/PNG/Default size/Environment/
    - assets/images/kenney_medieval-rts/PNG/Default size/Structure/
    - assets/images/kenney_medieval-rts/PNG/Default size/Unit/
    - assets/images/kenney_medieval-rts/PNG/Default size/Tile/
    # Game assets - Kenney Foliage Sprites
    - assets/images/kenney_foliage-sprites/PNG/Flat/
    - assets/images/kenney_foliage-sprites/PNG/Shaded/
    # Game assets - LPC Food sprites
    - assets/images/lpc-food-v2/
    # Game assets - Kenney UI Pack panels
    - assets/images/kenney_ui-pack/PNG/Blue/Default/
    - assets/images/kenney_ui-pack/PNG/Blue/Double/
    - assets/images/kenney_ui-pack/PNG/Grey/Default/
    # Game assets - Fish Pack (수영 게임: 물고기 수집, 해골물고기 장애물, 해조류 배경)
    - assets/images/kenney_fish-pack_2/PNG/Default/
    - assets/images/kenney_fish-pack_2/Spritesheet/
    # Game assets - Sports Pack (볼링 게임: 볼링공, 스포츠 장비)
    - assets/images/kenney_sports-pack/PNG/Equipment/
    - assets/images/kenney_sports-pack/Spritesheet/
    # Game assets - Rolling Ball (볼링 배경 타일)
    - assets/images/kenney_rolling-ball-assets/PNG/Default/
    - assets/images/kenney_rolling-ball-assets/Spritesheet/
    # Shield Guard Game sprites
    - assets/shield/
    # Shield Guard Game audio
    - assets/audio/sheild_guard/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
